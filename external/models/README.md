# Model Weights

Model weights are not stored in this repository.

Download the required embedding/OCR/vision models separately and place them
under this directory as needed by your local configuration. Keep large files
such as `.bin`, `.safetensors`, `.pt`, `.pth`, and `.onnx` out of Git.

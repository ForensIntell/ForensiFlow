# Public Template Examples

These JSON files are sanitized workflow examples. They are not loaded by default
and are not guaranteed to be directly runnable because generated scripts and
private registry entries are omitted from the public repository.

Use them as schema/reference material when building your own local RAG library.

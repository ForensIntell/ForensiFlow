# ForensiVision Weights

ForensiVision model weights are excluded from the public repository.

Place your local `clip_mdl.pth`, YOLO weights, and label files here when running
ForensiVision-backed perception locally. Do not commit these files.
